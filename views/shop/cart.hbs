<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{ pageTitle }}</title>
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>

    <main>
        <h1>Current items in the cart:</h1>
        <ul>

            {{#if hasProducts}}
            <div class="grid">
                {{#each prods}}
                <article class="item product-item">
                    <header class="item__header">
                        <h1 class="product__title">{{this.title}}</h1>
                    </header>
                    <div class="item__image">
                        <img src="{{this.imageUrl}}" alt="A Book">
                    </div>

                    <div class="item__content">
                        <h2 class="product__price">{{this.price}}$</h2>
                        <h3 class="product__price">Quantity: {{this.quantity}}</h3>
                    </div>

                    <div class="item__actions">
                        <a href="/admin/edit-product/{{this._id}}?edit=true" class="btn">Remove one</a>

                        <form action="/cart-delete-item" method="POST">
                            <input type="hidden" name="productId" id="" value="{{this._id}}">
                            <button class="btn" type="submit">Remove from Cart</button>
                        </form>
                    </div>
                </article>
                {{/each }}

            </div>

            <form action="/add-order" method="POST">

                <button class="btn" type="submit">Order Now!</button>
            </form>
            {{else}}
            <h1>No Products Found!</h1>
            {{/if}}

        </ul>
    </main>
</body>


</html>